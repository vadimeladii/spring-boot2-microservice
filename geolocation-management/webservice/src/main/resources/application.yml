spring:
  # Spring configuration
  main:
    allow-bean-definition-overriding: true

# Server configuration
server:
  port: ${random.int[8601,8700]}

# Eureka configuration
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/